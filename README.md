## Run Instructions

1. Open the Sleep Journal Project in Xcode
2. Choose an *iOS 26.1 Simulator* as the run target
3. Press 'Cmd + R' to build and run the app on the simulator
4. Allow location and photo library access when prompted if you want to test all entry creation features. 

No extra setup, package installation, or backend configuration is needed